<!DOCTYPE html>
<html>
<head>
    <title>My Donations</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">

<h2>My Donation History</h2>

<a href="/donor/dashboard" class="btn btn-secondary mb-3">Back to Dashboard</a>
<a href="/logout" class="btn btn-danger float-end">Logout</a>

<table class="table table-bordered mt-3">
    <thead>
        <tr>
            <th>Food</th>
            <th>Quantity</th>
            <th>Expiry Time</th>
            <th>Pickup Address</th>
            <th>Pickup Phone</th>
            <th>Created At</th>
            <th>Status</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        <% donations.forEach(donation => { %>
            <tr>
                <td><%= donation.food_description %></td>
                <td><%= donation.quantity %></td>
                <td><%= donation.expiry_time %></td>
                <td><%= donation.pickup_address %></td>
                <td><%= donation.pickup_phone %></td>
                <td><%= donation.created_at.toLocaleString() %></td>
                <td>
                    <% if (donation.status === "picked") { %>
                        <span class="badge bg-success">Picked</span>
                    <% } else if (donation.status === "requested") { %>
                        <span class="badge bg-warning">Requested</span>
                    <% } else { %>
                        <span class="badge bg-primary">Available</span>
                    <% } %>
                </td>
                <td><a href="/listing/<%= donation.id %>">See More</a></td>
            </tr>
        <% }) %>
    </tbody>
</table>

</body>
</html>