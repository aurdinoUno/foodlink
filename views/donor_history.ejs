<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Donations — FoodLink</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>

<body class="bg-light">

    <nav class="navbar navbar-dark bg-success">
        <div class="container">
            <a href="/donor/dashboard" class="navbar-brand" style="font-size:1rem;">← Dashboard</a>
            <span class="navbar-brand">🍃 FoodLink</span>
            <a href="/auth/logout" class="btn btn-outline-light btn-sm">Logout</a>
        </div>
    </nav>

    <div class="container py-4">
        <h4 class="page-title">My Donation History</h4>

        <% if (donations && donations.length> 0) { %>
            <div class="bg-white rounded-3 shadow-sm overflow-hidden">
                <table class="table table-hover mb-0">
                    <thead>
                        <tr>
                            <th>Food Item</th>
                            <th>Qty</th>
                            <th>Expiry</th>
                            <th>Pickup Address</th>
                            <th>Phone</th>
                            <th>Listed On</th>
                            <th>Status</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% donations.forEach(donation=> { %>
                            <tr>
                                <td class="fw-medium">
                                    <%= donation.food_description %>
                                </td>
                                <td>
                                    <%= donation.quantity %>
                                </td>
                                <td class="text-muted" style="font-size:0.82rem;">
                                    <%= new Date(donation.expiry_time).toLocaleString('en-IN', {dateStyle:'medium',
                                        timeStyle:'short'}) %>
                                </td>
                                <td class="text-muted"
                                    style="max-width:160px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">
                                    <%= donation.pickup_address %>
                                </td>
                                <td class="text-muted">
                                    <%= donation.pickup_phone %>
                                </td>
                                <td class="text-muted" style="font-size:0.82rem;">
                                    <%= new Date(donation.created_at).toLocaleDateString('en-IN', {dateStyle:'medium'})
                                        %>
                                </td>
                                <td>
                                    <% if (donation.status==='picked' ) { %>
                                        <span class="badge badge-picked">Picked Up</span>
                                        <% } else if (donation.status==='approved' ) { %>
                                            <span class="badge badge-approved">Approved</span>
                                            <% } else if (donation.status==='requested' ) { %>
                                                <span class="badge badge-requested">Requested</span>
                                                <% } else { %>
                                                    <span class="badge badge-available">Available</span>
                                                    <% } %>
                                </td>
                                <td><a href="/listing/<%= donation.id %>"
                                        class="btn btn-outline-secondary btn-sm">View</a></td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
            <% } else { %>
                <div class="bg-white rounded-3 shadow-sm p-5 text-center">
                    <div style="font-size:2.5rem;">🍱</div>
                    <h5 class="mt-3">No donations yet</h5>
                    <p class="text-muted">Start by creating your first food listing.</p>
                    <a href="/listing/create" class="btn btn-success">+ Create Listing</a>
                </div>
                <% } %>
    </div>
</body>

</html>