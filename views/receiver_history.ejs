<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Pickup Requests — FoodLink</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>

<body class="bg-light">

    <nav class="navbar navbar-dark bg-success">
        <div class="container">
            <a href="/receiver/dashboard" class="navbar-brand" style="font-size:1rem;">← Dashboard</a>
            <span class="navbar-brand">🍃 FoodLink</span>
            <a href="/auth/logout" class="btn btn-outline-light btn-sm">Logout</a>
        </div>
    </nav>

    <div class="container py-4">
        <h4 class="page-title">My Pickup Requests</h4>

        <% if (requests && requests.length> 0) { %>
            <div class="bg-white rounded-3 shadow-sm overflow-hidden">
                <table class="table table-hover mb-0">
                    <thead>
                        <tr>
                            <th>Donor</th>
                            <th>Food Item</th>
                            <th>Qty</th>
                            <th>Expiry</th>
                            <th>Pickup Address</th>
                            <th>Phone</th>
                            <th>Pickup Time</th>
                            <th>Status</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% requests.forEach(req=> { %>
                            <tr>
                                <td class="fw-medium text-success">
                                    <%= req.donor_name %>
                                </td>
                                <td class="fw-medium">
                                    <%= req.food_description %>
                                </td>
                                <td>
                                    <%= req.quantity %>
                                </td>
                                <td class="text-muted" style="font-size:0.82rem;">
                                    <%= new Date(req.expiry_time).toLocaleString('en-IN', {dateStyle:'medium',
                                        timeStyle:'short'}) %>
                                </td>
                                <td class="text-muted"
                                    style="max-width:160px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">
                                    <%= req.pickup_address %>
                                </td>
                                <td class="text-muted">
                                    <%= req.pickup_phone %>
                                </td>
                                <td class="text-muted" style="font-size:0.82rem;">
                                    <%= new Date(req.pickup_time).toLocaleString('en-IN', {dateStyle:'medium',
                                        timeStyle:'short'}) %>
                                </td>
                                <td>
                                    <% if (req.status==='picked' ) { %>
                                        <span class="badge badge-picked">Picked Up</span>
                                        <% } else if (req.status==='approved' ) { %>
                                            <span class="badge badge-approved">Approved</span>
                                            <% } else if (req.status==='requested' ) { %>
                                                <span class="badge badge-requested">Requested</span>
                                                <% } else { %>
                                                    <span class="badge badge-available">Available</span>
                                                    <% } %>
                                </td>
                                <td><a href="/listing/<%= req.id %>" class="btn btn-outline-secondary btn-sm">View</a>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
            <% } else { %>
                <div class="bg-white rounded-3 shadow-sm p-5 text-center">
                    <div style="font-size:2.5rem;">🔍</div>
                    <h5 class="mt-3">No requests yet</h5>
                    <p class="text-muted">Browse the dashboard to claim available food listings.</p>
                    <a href="/receiver/dashboard" class="btn btn-success">Browse Listings</a>
                </div>
                <% } %>
    </div>
</body>

</html>